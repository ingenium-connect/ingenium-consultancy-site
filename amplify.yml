version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 24.16.0
    build:
      commands:
        - npm install
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    - node_modules/.cache
    - .next/cache
  buildTimeout: 30
